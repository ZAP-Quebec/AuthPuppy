<h2><?php echo __('Database creation/update'); ?></h2>

<p><?php echo __("By clicking \"Next\", you'll start the migration process to <strong>create / update the db</strong> to its actual version.")?></p>
<p><?php echo __("The <strong>database must exist</strong> and must be <strong>configured</strong> in <i>config/databases.yml</i> (a default example can be found in config/databases.yml.default)")?></p>

<p class="step"><input type="submit" name="submit[save]" value="<?php echo __("Next")?>" class="button" /></p>
